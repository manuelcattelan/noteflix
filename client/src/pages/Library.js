import React from 'react';
import { Container } from 'react-bootstrap';

const Library = (props) => {
    return (
        <>
            <Container>
                library
            </Container>
            
        </>
    );
};

export default Library;